model:
  type: AE_DeepSDF
  params:
    latent_dim: 9
    hidden_dim: 128
    tau_latent_dim: 2
    tau_loss_weight: 0.1
    orthogonality_loss_weight: 0.1
    regularization: l2
    reg_weight: 0.001

trainer:
  learning_rate: 0.0001
  reg_weight: 0.001
  regularization: l2
  warmup_steps: 1000